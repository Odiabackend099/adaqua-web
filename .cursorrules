# HARD RULES  DO NOT CHANGE
- Use pages/ router only (single UI).
- Do not call tts-api.odia.dev from the browser; only server-side via /api/tts using TTS_API_BASE.
- No fallbacks. If TTS fails, surface the error; do not synthesize locally.